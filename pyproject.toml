[project]
name = "sorteador"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Robert-FM", email = "robertfmelo29@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
sorteador = "sorteador:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
